package com.entity;

public class Bulkheaddata {
	private String buno;
	private String amount;
	private String buname;
	
	public Bulkheaddata(String buno, String amount, String buname) {
		super();
		this.buno = buno;
		this.amount = amount;
		this.buname = buname;
	}
	public Bulkheaddata() {
		super();
		// TODO Auto-generated constructor stub
	}
	public String getBuno() {
		return buno;
	}
	public void setBuno(String buno) {
		this.buno = buno;
	}
	public String getAmount() {
		return amount;
	}
	public void setAmount(String amount) {
		this.amount = amount;
	}
	public String getBuname() {
		return buname;
	}
	public void setBuname(String buname) {
		this.buname = buname;
	}
	@Override
	public String toString() {
		return "Bulkhead [buno=" + buno + ", amount=" + amount
				+ ", buname=" + buname + "]";
	}

}


package com.dao.show;

import java.sql.SQLException;
import java.util.List;

import org.apache.commons.dbutils.QueryRunner;
import org.apache.commons.dbutils.handlers.BeanListHandler;

import com.entity.Broadsidedata;
import com.entity.Bulkheaddata;
import com.util.JdbcUtil;

public class ShowBulkheadDaoImpl implements ShowBulkheadDao {

	public List<Bulkheaddata> showData(String tablename) {
		try {
			QueryRunner qr = new QueryRunner(JdbcUtil.getDataSource());
			String sql = "SELECT * FROM "+tablename+" ";
			return (List<Bulkheaddata>)qr.query(sql, new BeanListHandler(Bulkheaddata.class));
		} catch (SQLException e) {
			e.printStackTrace();
			throw new RuntimeException(e);
		}
	}
}


package com.service.show;

import java.util.List;

import com.dao.show.ShowBulkheadDao;
import com.dao.show.ShowBulkheadDaoImpl;
import com.entity.Bulkheaddata;

public class ShowBulkheadServiceImpl implements ShowBulkheadService {

	ShowBulkheadDao dao = new ShowBulkheadDaoImpl();
	public List<Bulkheaddata> showData(String tablename) {	
		System.out.println(dao.showData(tablename));
		return dao.showData(tablename);
	}

}


package com.web.show;

import java.io.IOException;
import java.util.List;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.entity.Alltabledata;
import com.entity.Bulkheaddata;
import com.service.show.ShowBulkheadService;
import com.service.show.ShowBulkheadServiceImpl;
import com.service.show.ShowDataService;
import com.service.show.ShowDataServiceImpl;

public class ShowBulkheadServlet extends HttpServlet {
	public void doGet(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		request.setCharacterEncoding("utf-8");
		// 数据封装
	
		
		//将用户输入的数据封装到aquery
		//aquery = WebUtil.copyRequestToBean(request, AdminQuery.class);
		// 请求头用于防止非法链接
		String referer = request.getHeader("referer");
		String tablename = request.getParameter("tablename");
		
		// 获取用输入的数据
		String currentPage = request.getParameter("currentPage");
		if (currentPage == null || currentPage.equals("")) {
			// 如果用户第一次访问，没有传递currentPage参数，则当前页为1
			currentPage = "1";
		}

		String pageSize = request.getParameter("pageSize");
		if (pageSize == null || pageSize.equals("")) {
			pageSize = "5";
		}
		ShowBulkheadService service = new ShowBulkheadServiceImpl();
		List<Bulkheaddata> bulist = service.showData(tablename);
		System.out.println(bulist);
		//request.setAttribute("pageBean", pageBean);
		request.setAttribute("bulist", bulist);
		request.getRequestDispatcher("/jsp/bulkhead.jsp").forward(request, response);	

			
	}

	

	public void doPost(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		doGet(request, response);

	}


}

