package com.entity;

public class Shiyanfenduandata {
	private String seno;
	private String amount;
	private String shname;
	
	public Shiyanfenduandata(String seno,String amount,String shname){
		super();
		this.seno=seno;
		this.amount=amount;
		this.shname=shname;
	}
	
	public Shiyanfenduandata(){
		super();
	}

	public String getSeno() {
		return seno;
	}

	public void setSeno(String seno) {
		this.seno = seno;
	}

	public String getAmount() {
		return amount;
	}

	public void setAmount(String amount) {
		this.amount = amount;
	}

	public String getShname() {
		return shname;
	}

	public void setShname(String shname) {
		this.shname = shname;
	}

	@Override
	public String toString() {
		return "Shiyan_view_fenduan [seno=" + seno + ", amount=" + amount
				+ ", shname=" + shname + "]";
	}

}

package com.dao.show;

import java.sql.SQLException;
import java.util.List;

import org.apache.commons.dbutils.QueryRunner;
import org.apache.commons.dbutils.handlers.BeanListHandler;
import com.entity.Shiyanfenduandata;
import com.util.JdbcUtil;

public class ShowShiyanfenduanDaoImpl implements ShowShiyanfenduanDao {

	public List<Shiyanfenduandata> showData(String tablename) {
		try {
			QueryRunner qr = new QueryRunner(JdbcUtil.getDataSource());
			String sql = "SELECT * FROM "+tablename+" ";
			return (List<Shiyanfenduandata>)qr.query(sql, new BeanListHandler(Shiyanfenduandata.class));
		} catch (SQLException e) {
			e.printStackTrace();
			throw new RuntimeException(e);
		}
	}


}

package com.service.show;

import java.util.List;

import com.dao.show.ShowShiyanfenduanDao;
import com.dao.show.ShowShiyanfenduanDaoImpl;

import com.entity.Shiyanfenduandata;


public class ShowShiyanfenduanServiceImpl implements ShowShiyanfenduanService {


	ShowShiyanfenduanDao dao = new ShowShiyanfenduanDaoImpl();
	public List<Shiyanfenduandata> showData(String tablename) {	
		System.out.println(dao.showData(tablename));
		return dao.showData(tablename);
	}


}

package com.web.show;

import java.io.IOException;
import java.util.List;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.entity.Shiyanfenduandata;
import com.entity.Zhunbeichukudata;
import com.service.show.ShowShiyanfenduanService;
import com.service.show.ShowShiyanfenduanServiceImpl;
import com.service.show.ShowZhunbeichukuService;
import com.service.show.ShowZhunbeichukuServiceImpl;

public class ShowShiyanfenduanServlet extends HttpServlet {
	public void doGet(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		request.setCharacterEncoding("utf-8");
		// 数据封装
	
		
		//将用户输入的数据封装到aquery
		//aquery = WebUtil.copyRequestToBean(request, AdminQuery.class);
		// 请求头用于防止非法链接
		String referer = request.getHeader("referer");
		String tablename = request.getParameter("tablename");
		
		// 获取用输入的数据
		String currentPage = request.getParameter("currentPage");
		if (currentPage == null || currentPage.equals("")) {
			// 如果用户第一次访问，没有传递currentPage参数，则当前页为1
			currentPage = "1";
		}

		String pageSize = request.getParameter("pageSize");
		if (pageSize == null || pageSize.equals("")) {
			pageSize = "5";
		}
		ShowShiyanfenduanService service = new ShowShiyanfenduanServiceImpl();
		List<Shiyanfenduandata> shflist = service.showData(tablename);
		System.out.println(shflist);
		//request.setAttribute("pageBean", pageBean);
		request.setAttribute("shflist", shflist);
		request.getRequestDispatcher("/jsp/shiyanfenduan.jsp").forward(request, response);	

			
	}

	public void doPost(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		doGet(request, response);

	}

}

