package com.entity;

public class Fenduanchukudata {
	private String seno;
	private String amount;
	private String zhname;
	
	public Fenduanchukudata(String seno,String amount,String zhname){
		super();
		this.seno=seno;
		this.amount=amount;
		this.zhname=zhname;
	}
	
	public Fenduanchukudata(){
		super();
	}

	public String getSeno() {
		return seno;
	}

	public void setSeno(String seno) {
		this.seno = seno;
	}

	public String getAmount() {
		return amount;
	}

	public void setAmount(String amount) {
		this.amount = amount;
	}

	public String getZhname() {
		return zhname;
	}

	public void setZhname(String zhname) {
		this.zhname = zhname;
	}

	@Override
	public String toString() {
		return "Fenduan_view_chuku [seno=" + seno + ", amount=" + amount
				+ ", zhname=" + zhname + "]";
	}
	
}

package com.dao.show;

import java.sql.SQLException;
import java.util.List;

import org.apache.commons.dbutils.QueryRunner;
import org.apache.commons.dbutils.handlers.BeanListHandler;

import com.entity.Fenduanchukudata;

import com.util.JdbcUtil;

public class ShowFenduanchukuDaoImpl implements ShowFenduanchukuDao {

	public List<Fenduanchukudata> showData(String tablename) {
		try {
			QueryRunner qr = new QueryRunner(JdbcUtil.getDataSource());
			String sql = "SELECT * FROM "+tablename+" where amount !=0 ";
			return (List<Fenduanchukudata>)qr.query(sql, new BeanListHandler(Fenduanchukudata.class));
		} catch (SQLException e) {
			e.printStackTrace();
			throw new RuntimeException(e);
		}
	}

}

package com.service.show;

import java.util.List;

import com.dao.show.ShowFenduanchukuDao;
import com.dao.show.ShowFenduanchukuDaoImpl;
import com.dao.show.ShowZhunbeichukuDao;
import com.dao.show.ShowZhunbeichukuDaoImpl;
import com.entity.Fenduanchukudata;
import com.entity.Zhunbeichukudata;

public class ShowFenduanchukuServiceImpl implements ShowFenduanchukuService {

	ShowFenduanchukuDao dao = new ShowFenduanchukuDaoImpl();
	public List<Fenduanchukudata> showData(String tablename) {	
		System.out.println(dao.showData(tablename));
		return dao.showData(tablename);
	}

}

package com.web.show;

import java.io.IOException;
import java.util.List;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.entity.Fenduanchukudata;
import com.entity.Zhunbeichukudata;
import com.service.show.ShowFenduanchukuService;
import com.service.show.ShowFenduanchukuServiceImpl;
import com.service.show.ShowZhunbeichukuService;
import com.service.show.ShowZhunbeichukuServiceImpl;

public class ShowFenduanchukuServlet extends HttpServlet {
	public void doGet(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		request.setCharacterEncoding("utf-8");
		// 数据封装
	
		
		//将用户输入的数据封装到aquery
		//aquery = WebUtil.copyRequestToBean(request, AdminQuery.class);
		// 请求头用于防止非法链接
		String referer = request.getHeader("referer");
		String tablename = request.getParameter("tablename");
		
		// 获取用输入的数据
		String currentPage = request.getParameter("currentPage");
		if (currentPage == null || currentPage.equals("")) {
			// 如果用户第一次访问，没有传递currentPage参数，则当前页为1
			currentPage = "1";
		}

		String pageSize = request.getParameter("pageSize");
		if (pageSize == null || pageSize.equals("")) {
			pageSize = "5";
		}
		ShowFenduanchukuService service = new ShowFenduanchukuServiceImpl();
		List<Fenduanchukudata> feclist = service.showData(tablename);
		System.out.println(feclist);
		//request.setAttribute("pageBean", pageBean);
		request.setAttribute("feclist", feclist);
		request.getRequestDispatcher("/jsp/fenduanchuku.jsp").forward(request, response);	

			
	}

	public void doPost(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		doGet(request, response);

	}


}

