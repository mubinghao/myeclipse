package com.entity;

public class Zhunbeichukudata {
	private String seno;
	private String amount;
	private String zhname;
	
	public Zhunbeichukudata(String seno,String amount,String zhname){
		super();
		this.seno=seno;
		this.amount=amount;
		this.zhname=zhname;
	}
	
	public Zhunbeichukudata(){
		super();
	}

	public String getSeno() {
		return seno;
	}

	public void setSeno(String seno) {
		this.seno = seno;
	}

	public String getAmount() {
		return amount;
	}

	public void setAmount(String amount) {
		this.amount = amount;
	}

	public String getZhname() {
		return zhname;
	}

	public void setZhname(String zhname) {
		this.zhname = zhname;
	}

	@Override
	public String toString() {
		return "Zhunbei_view_chuku [seno=" + seno + ", amount=" + amount
				+ ", zhname=" + zhname + "]";
	}
	
}

package com.dao.show;

import java.sql.SQLException;
import java.util.List;

import org.apache.commons.dbutils.QueryRunner;
import org.apache.commons.dbutils.handlers.BeanListHandler;


import com.entity.Zhunbeichukudata;
import com.util.JdbcUtil;

public class ShowZhunbeichukuDaoImpl implements ShowZhunbeichukuDao {
	public List<Zhunbeichukudata> showData(String tablename) {
		try {
			QueryRunner qr = new QueryRunner(JdbcUtil.getDataSource());
			String sql = "SELECT * FROM "+tablename+" where amount !=0 ";
			return (List<Zhunbeichukudata>)qr.query(sql, new BeanListHandler(Zhunbeichukudata.class));
		} catch (SQLException e) {
			e.printStackTrace();
			throw new RuntimeException(e);
		}
	}



}

package com.service.show;

import java.util.List;


import com.dao.show.ShowZhunbeichukuDao;
import com.dao.show.ShowZhunbeichukuDaoImpl;
import com.entity.Zhunbeichukudata;

public class ShowZhunbeichukuServiceImpl implements ShowZhunbeichukuService {

	ShowZhunbeichukuDao dao = new ShowZhunbeichukuDaoImpl();
	public List<Zhunbeichukudata> showData(String tablename) {	
		System.out.println(dao.showData(tablename));
		return dao.showData(tablename);
	}


}

package com.web.show;

import java.io.IOException;
import java.util.List;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.entity.Deckdata;
import com.entity.Zhunbeichukudata;
import com.service.show.ShowDeckService;
import com.service.show.ShowDeckServiceImpl;
import com.service.show.ShowZhunbeichukuService;
import com.service.show.ShowZhunbeichukuServiceImpl;

public class ShowZhunbeichukuServlet extends HttpServlet {
	public void doGet(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		request.setCharacterEncoding("utf-8");
		// 数据封装
	
		
		//将用户输入的数据封装到aquery
		//aquery = WebUtil.copyRequestToBean(request, AdminQuery.class);
		// 请求头用于防止非法链接
		String referer = request.getHeader("referer");
		String tablename = request.getParameter("tablename");
		
		// 获取用输入的数据
		String currentPage = request.getParameter("currentPage");
		if (currentPage == null || currentPage.equals("")) {
			// 如果用户第一次访问，没有传递currentPage参数，则当前页为1
			currentPage = "1";
		}

		String pageSize = request.getParameter("pageSize");
		if (pageSize == null || pageSize.equals("")) {
			pageSize = "5";
		}
		ShowZhunbeichukuService service = new ShowZhunbeichukuServiceImpl();
		List<Zhunbeichukudata> zhclist = service.showData(tablename);
		System.out.println(zhclist);
		//request.setAttribute("pageBean", pageBean);
		request.setAttribute("zhclist", zhclist);
		request.getRequestDispatcher("/jsp/zhunbeichuku.jsp").forward(request, response);	

			
	}

	public void doPost(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		doGet(request, response);

	}


}
